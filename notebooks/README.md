# Notebooks

This folder contains a portfolio walkthrough notebook.

The original coursework notebook had a much longer analysis flow with many outputs. For GitHub, the main reproducible code has been moved into the `src/` folder, while this notebook gives a readable walkthrough of the project and links to the saved visual outputs.

To rerun the full workflow, download the Kaggle dataset and use:

```bash
python src/main.py
```
